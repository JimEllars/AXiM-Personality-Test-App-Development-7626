- Telemetry Hardening: Enabled sessionStorage buffering in telemetry.js, ensuring unsent payloads gracefully survive navigation or app unmounts without loss. Enforced 64KB upload restrictions on Edge Worker.
- Input Polish & Accessibility: Verified explicit ARIA attributes across `TradeoffSliderInput`, `ScenarioCardInput`, and `ReactionDilemmaInput`. Improved focus states and keyboard arrow navigation limits. Standardized 44x44px minimum tap targets.
- Export Reliability: Added explicit defensive error handling to `ResultsToolbar.jsx` PDF download link to safely prevent UI freezing on network failure or generation exceptions.
